#include <stdio.h>
#include "main.h"

/**
 *add - a function that adds two integers
 *@a: an input
 *@b: an input
 *Return: returns a value
 */
int add(int a, int b)

{
int z;
z = a + b;
return (z);
}
